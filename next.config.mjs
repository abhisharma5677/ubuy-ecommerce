/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'], // Add the hostname(s) of your image provider here
    },
};

export default nextConfig;
